# Результати

Архітектура: **ResNet-18** (torchvision, попередньо натренована на ImageNet-1k),
дотренована на **imagenet-10** (10 класів × 1300 зображень, стратифікований
train/val split 85/15, seed=42). Оцінка нижче — на val-наборі (~195 зображень),
який модель не бачила під час тренування.

Натреновано дві моделі для порівняння:

- **baseline** — звичайне тренування, тільки на чистих зображеннях
  (`--w-clean 1.0 --w-noise 0 --w-adv 0`)
- **robust** — adversarial-тренування: на кожному кроці комбінуються втрати
  на чистих, зашумлених (Gaussian Noise) і FGSM-adversarial зображеннях
  (`--w-clean 0.34 --w-noise 0.33 --w-adv 0.33`, eps на тренуванні
  випадковий з `U(2/255, 8/255)`)

Обидві тренувались 6 епох, `lr=3e-4`, `AdamW`, `batch_size=32`.

## 1. Базова якість і стійкість до шуму

| Модель | Clean accuracy | Gaussian Noise accuracy (p=1.0, σ~U(0.01,0.08)) |
|---|---|---|
| baseline | 97.6% | 93.5% |
| robust | 95.2% | **94.9%** |

Обидві моделі виявились доволі стійкими до Gaussian Noise атаки навіть без
спеціального тренування — це очікувано, бо RandomResizedCrop/flip уже
частково "загартовують" модель проти дрібних пертурбацій, а сама амплітуда
шуму (σ ≤ 0.08) відносно невелика. Робастна модель трохи стійкіша (94.9%
проти 93.5%) і водночас має трохи нижчу чисту точність (95.2% проти
97.6%) — це типовий trade-off adversarial training: невелика "плата" у
чистій точності за стійкість.

## 2. FGSM epsilon sweep — головний результат

Повна крива точність-vs-eps для обох моделей:

| eps (÷255) | baseline accuracy | robust accuracy |
|---|---|---|
| 0 | 97.6% | 95.2% |
| 1 | 64.6% | 92.4% |
| 2 | 45.3% | 88.6% |
| 4 | 32.0% | 81.4% |
| 6 | 28.3% | 75.5% |
| 8 | 27.1% | 68.2% |
| 12 | 27.7% | 56.9% |
| 16 | 27.8% | 48.1% |
| 24 | 25.6% | 37.3% |
| 32 | 22.0% | 29.3% |
| 48 | 13.9% | 18.5% |
| 64 | 11.2% | 12.8% |
| 96 | 10.5% | 10.1% |
| 128 | 10.8% | 9.9% |

Графіки: `src/runs/robust/fgsm_sweep.png`, `src/runs/baseline/fgsm_sweep.png`,
порівняльний `src/runs/fgsm_comparison.png`.

### Межа eps, при якій модель "перестає працювати"

Визначено два порогові критерії (10 класів → рівень випадкового вгадування = 10%):

| Критерій | baseline | robust |
|---|---|---|
| Точність падає нижче 50% від чистої | **eps ≈ 2/255 (0.008)** | **eps ≈ 24/255 (0.094)** |
| Точність колапсує до рівня випадкового вгадування (~10%) | **eps ≈ 48/255 (0.188)** | **eps ≈ 64/255 (0.251)** |

**Висновок:** baseline-модель фактично ламається вже при мінімальному
збуренні — вже на eps=1/255 (зміна яскравості пікселя на 1 з 255 рівнів,
візуально майже непомітна) точність падає з 97.6% до 64.6%, а при eps=2/255
опускається нижче половини від чистої точності. Далі точність baseline
"плато" тримається на рівні ~25-28% аж до eps≈32/255 (це трохи вище
випадкового вгадування — модель частково впізнає найлегші класи), і лише
після eps≈48/255 остаточно колапсує до ~10-11%.

Robust-модель, натренована на FGSM + Gaussian Noise, витримує значно
сильніші атаки: практичний злам (падіння вдвічі) настає лише при
**eps ≈ 24/255**, а повний колапс до рівня випадкового вгадування — при
**eps ≈ 64/255**, тобто **приблизно у 8 разів вища** межа стійкості
порівняно з baseline (24/255 проти 2/255) за критерієм "50% від чистої
точності".

## 3. Висновки

1. Adversarial training (FGSM зі випадковим eps на тренуванні) +
   noise-augmentation суттєво підвищують робастність моделі до FGSM-атак
   ціною невеликого (~2.5 п.п.) падіння чистої точності.
2. Стійкість до Gaussian Noise атаки в обох моделях висока навіть без
   спеціального тренування проти неї, оскільки амплітуда шуму (σ до 0.08)
   відносно мала порівняно з типовими FGSM-збуреннями такого ж масштабу.
3. "Ліміт eps, при якому модель перестає працювати" — залежить від
   визначення "перестає працювати":
   - як практичний злам (точність < 50% від чистої): **eps ≈ 24/255** для robust-моделі;
   - як повний колапс (точність ≈ випадкове вгадування): **eps ≈ 64/255** для robust-моделі.

## Артефакти

- `src/runs/robust/best_model.pt`, `src/runs/baseline/best_model.pt` — чекпоінти моделей
- `src/runs/robust/eval_results.json`, `src/runs/baseline/eval_results.json` — повні числові результати
- `src/runs/robust/fgsm_sweep.png`, `src/runs/baseline/fgsm_sweep.png` — графіки для кожної моделі окремо
- `src/runs/fgsm_comparison.png` — порівняльний графік baseline vs robust
